"""Scoring application services: the gap-safe scoreboard projector (M7).

Import ``ScoreProjector`` from ``.projector`` (no re-export here: the
projector pulls the optional ``[db]`` extra, and this package must stay
importable without it).
"""
