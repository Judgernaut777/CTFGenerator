"""Competition domain: the pure event-log contract (``Event``, ``Clock``,
``EventStore``) and the volatile ``InMemoryEventStore``. File- and
database-backed stores live in ``ctf_generator.infrastructure``. See
``events`` for the canonical contract; ``ctf_generator.events`` is a
compatibility shim re-exporting from here.
"""
