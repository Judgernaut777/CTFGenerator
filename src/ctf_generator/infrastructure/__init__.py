"""Infrastructure layer: concrete implementations of application/domain
protocols -- filesystem, container runtime, artifact stores, database, queues,
providers, authentication, telemetry. Nothing in ``domain`` may import this.
"""
